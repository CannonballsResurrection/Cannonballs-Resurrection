.build/
.DS_Store
snapshots/
resurrection-archive/game-files/
*.qcow2
*.vhd
*.iso
__pycache__/
.claude/settings.local.json
# trailer render scratch + output (multi-GB PNG sequences, intermediates, final mp4)
trailer/

# build artifacts / packaged installers (shipped via GitHub Releases, not git)
dist/
windows/build/
windows/dist/

# throwaway publish secrets — never commit
tools/publish-public.env

# transient single-file-build asset pack
windows/assets.pck
