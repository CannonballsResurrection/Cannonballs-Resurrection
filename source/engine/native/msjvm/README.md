# Microsoft JVM (msjavx86)

`msjavx86.zip` — the **Microsoft Java Virtual Machine**, the JRE WebDriver applets
ran under. Microsoft discontinued it (support ended 2007); it's preserved here
because the original game cannot load without it. © Microsoft.
